import { Component, OnInit } from '@angular/core';

@Component({
    selector: 'health-card-cmp',
    moduleId: module.id,
    templateUrl: 'health-card.component.html'
})

export class HealthCardComponent implements OnInit{
    ngOnInit(){
    }
}
